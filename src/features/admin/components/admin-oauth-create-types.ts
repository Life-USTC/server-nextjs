export type AuthPatternOption = {
  descriptionKey: string;
  hintKey: string;
  labelKey: string;
  titleKey: string;
  value: string;
};

export type ScopeOption = {
  descriptionKey: string;
  value: string;
};
